---
import Nav from "./Nav.astro";

export interface Props {
  active: "home" | "projects" | "blog";
}

const { active } = Astro.props as Props;
---

<header class="container mx-auto max-w-5xl px-4 pt-4 flex justify-between">
  <h1>JasperDeLanghe</h1>
  <Nav active={active} />
</header>
